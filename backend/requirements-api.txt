# TruthTrace API — 精简生产依赖 (Render 免费层 512MB 优化)
# 完整依赖见 requirements.txt (Worker 服务使用)

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# 数据库
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# 缓存 & 队列
redis==5.1.1
celery[redis]==5.4.0

# 爬虫 (轻量 — playwright 单独安装)
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0

# NLP (轻量 — spacy/sentence-transformers 单独安装)
jieba==0.42.1

# 图像处理
Pillow==10.4.0
imagehash==4.3.1

# 工具
python-multipart==0.0.9
tenacity==9.0.0
loguru==0.7.2
orjson==3.10.7

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 速率限制
slowapi==0.1.9

# 监控 (可选 — Sentry DSN 未设时自动跳过)
sentry-sdk[fastapi]==2.14.0

# 测试 (生产环境跳过)
# pytest==8.3.3 — 生产不需要
# pytest-asyncio==0.24.0 — 生产不需要
# aiosqlite==0.20.0 — 生产用 PostgreSQL
